name: Assign
on:
  issue_comment:
    types: [created, edited]

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event }}"
      - name: take an issue
        uses: bdougie/take-action@main
