name: Take Actions
on:
  issue_comment:
    types: [created, edited, deleted]

jobs:
  take-issue:
    runs-on: ubuntu-latest
    steps:
      - name: take an issue
        uses: bdougie/take--action@debug
