name: Take Actions
on:
  issue_comment:
    types: [created, edited, deleted]

jobs:
  take-issue:
    runs-on: ubuntu-latest
    steps:
      - name: take an issue
        uses: crazy-max/take-action@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
